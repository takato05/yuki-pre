# PHPの公式イメージ（Apacheが同梱されているもの）をベースにする
FROM php:8.1-apache

# Apacheのドキュメントルート（/var/www/html）をプロジェクトルートとして使用

# ホストからコンテナへファイルをコピーする（今回はマウントを使うので必須ではないが、環境定義として記述）
# COPY . /var/www/html/

# コンテナがリッスンするポートを公開する
EXPOSE 80